<section class="page-align" id="footer">
    <h2>Edit Coupon</h2>
   <form action="/admin/editcouponsubmit/{{coupon._id}}" method="post">
                <div class="row mb-3 mt-5">
                  <label for="inputEmail3" class="col-sm-2 col-form-label">Coupon Name</label>
                  <div class="col-sm-10">
                    <input type="text" class="form-control" value="{{coupon.name}}"  name="name">
                  </div>
                </div>
                <div class="row mb-3">
                  <label for="inputEmail3" class="col-sm-2 col-form-label">Coupon Code/ID</label>
                  <div class="col-sm-10">
                    <input type="text" class="form-control" name="code" value="{{coupon.code}}" id="id">
                    <p onclick="codeGenerator()" class="badge badge-secondary">generate Code</p>
                  </div>
                </div>
                <div class="row mb-3">
                  <label for="inputPassword3" class="col-sm-2 col-form-label">Start Date</label>
                  <div class="col-sm-10">
                    <input type="date" class="form-control" name="startDate" value="{{coupon.startDate}}">
                  </div>
                </div>
                 <div class="row mb-3">
                  <label for="inputPassword3" class="col-sm-2 col-form-label">End Date</label>
                  <div class="col-sm-10">
                    <input type="date" class="form-control" name="endDate" value="{{coupon.endDate}}">
                  </div>
                </div>
                <div class="row mb-3 mt-5">
                  <label for="inputEmail3" class="col-sm-2 col-form-label">Percentage</label>
                  <div class="col-sm-10">
                    <input type="number"  onKeyPress="if(this.value.length==2) return false;" class="form-control" value="{{coupon.percentage}}" name="percentage">
                  </div>
                 <div class="row mb-3 mt-5">
                  <label for="inputEmail3" class="col-sm-2 col-form-label">Purchase Limit Amount</label>
                  <div class="col-sm-10">
                    <input type="text" class="form-control" value="{{coupon.limit}}" name="limit">
                  </div>
                

                <div class="text-center">
                  <button type="submit" class="btn btn-primary">Edit</button>
                </div>
              </form>
</section>
<script src="https://code.jquery.com/jquery-3.1.1.min.js"></script>
<script>
    function codeGenerator(){

        $.ajax({
            url:'/admin/generatecode',
            method:'get',
            success:(response => {
            document.getElementById('id').value = response

            })
        })
    
    }
</script>